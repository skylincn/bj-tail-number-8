# 每日 6 点限行提醒（本机自动版）

本项目除了「红色日历订阅」外，还附带一个**本机自动提醒**：每天 06:00 检查当天是否为尾号 8 限行日，是则弹出一个**必须点击才消失**的 Alert（不点就一直挂着，防止忘记）。

判定逻辑完全复用 `dist/plate-8.ics`（红色限行日历），所以限行星期每季度轮换、周末/法定节假日自动排除，本脚本**无需手动维护**。

---

## 文件说明

| 文件 | 作用 |
|------|------|
| `scripts/check_limit_today.py` | 限行日判定 + 弹窗脚本 |
| `com.bjtailnumber.check.plist`（在 `~/Library/LaunchAgents/`） | 每天 06:00 触发脚本的定时任务 |

---

## 在你本机激活（只需一次，一行命令）

> 注意：定时任务必须由**你本人本机**激活（需要 macOS 真实 GUI 会话上下文，远程 shell 无法加载）。在「终端」App 里粘贴：

```bash
launchctl load ~/Library/LaunchAgents/com.bjtailnumber.check.plist
```

加载后即时生效，每天 06:00 自动跑。Mac 若当时处于睡眠/关机，唤醒后会补跑一次。

### 验证

```bash
# 测试某天是否限行（不弹窗）
python3 scripts/check_limit_today.py --test 2026-08-12 --dry-run
# 应为：2026-08-12（周三）是限行日

# 手动触发一次真实弹窗（今天是限行日才会弹）
python3 scripts/check_limit_today.py
```

### 停用

```bash
launchctl unload ~/Library/LaunchAgents/com.bjtailnumber.check.plist
```

---

## 关于「iPhone 上也想收到」

本机（Mac）弹窗只在 Mac 开机时有效。若你希望**手机上每天 6 点也蹦提醒、点了才消失**，需在 iPhone「快捷指令」App 里建一个自动化——这步必须你本人在手机上点（Apple 不允许远程注入个人自动化）。配置方法见 README「第 5 章 每日 6 点提醒（快捷指令 + 提醒事项）」，思路与本脚本一致：读当天红色限行日历 → 有事件就建提醒事项（点了才消失）。
